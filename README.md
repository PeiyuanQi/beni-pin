# beni-pin

This repository is initialized for agentic development. Its product scope and technical stack are not documented yet.

## Development Flow

- Read `AGENTS.md` before using a coding agent in this repository.
- Prefer a git worktree for parallel or unrelated work. Project-local worktrees belong under `.worktrees/`.
- Setup: not documented yet.
- Run locally: not documented yet.
- Test, lint, and build: not documented yet.
- For documentation-only changes, run `git diff --check`.

When project tooling is introduced, replace the undocumented entries above with the repository's actual commands and keep `AGENTS.md` aligned.
